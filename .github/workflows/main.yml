name: Waka Readme

on:
  schedule:
    # Runs at 12am IST
    - cron: '30 18 * * *'
  workflow_dispatch:
jobs:
  update-readme:
    name: Update Readme with Metrics
    runs-on: ubuntu-latest
    steps:
      - uses: oscaralejandro1907/oscaralejandro1907@master
        with:
          WAKATIME_API_KEY: ${{ secrets.waka_95f402eb-773b-49a5-bbfd-bbb658cc9cb4 }}
          GH_TOKEN: ${{ secrets.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 }}
